This is a C-based steganography project that hides secret data inside BMP images using the Least Significant Bit (LSB) technique. 
The project supports password-protected encoding to securely embed data and password-based decoding to ensure only authorized users can extract the hidden content.
The decoded data can be saved in any user-preferred file format, while the hidden information remains invisible inside the stego-image.
