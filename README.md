# Steganography_lsb_project
A C-based project that hides and extracts secret data inside BMP images using LSB steganography, supports password-protected encoding/decoding, and allows the hidden data to be saved as user-preferred file types.
